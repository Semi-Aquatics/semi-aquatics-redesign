import styles from './StoryPage.module.scss';

const StoryPage: React.FC = () => (
  <div className={styles.storyPageContainer}>
    <h1 className={styles.storyTitle}>Story Page</h1>
  </div>
);

export default StoryPage;
